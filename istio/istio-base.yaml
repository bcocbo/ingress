apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: istio-base
  namespace: argocd
  finalizers:
    - argocd.argoproj.io/resources-finalizer
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: istio-system
  source:
    repoURL: https://istio-release.storage.googleapis.com/charts
    chart: base
    targetRevision: 1.23.*
    helm:
      values: |
        global:
          istioNamespace: istio-system
        defaultPodDisruptionBudget:
          enabled: true
        pilot:
          autoscaleEnabled: false
          nodeSelector:
            karpenter.sh/nodepool: default  # Target the default NodePool
          resources:
            requests:
              cpu: "500m"
              memory: "2048Mi"  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true